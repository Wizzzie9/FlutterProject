name: distance_check_app
description: "DistanceCheckApp"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_core: ^3.1.1
  firebase_database: ^11.0.2
  firebase_messaging: ^15.0.2
  firebase_analytics: ^11.1.0
  geolocator: ^12.0.0
  baseflow_plugin_template: ^2.1.2
  google_maps_flutter: ^2.7.0
  geocoding: ^3.0.0
  firebase_auth: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
